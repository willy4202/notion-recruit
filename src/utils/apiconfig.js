// api들을 모두 관리할 수 있는 페이지가 있으면 좋을 듯 함.
const API_BASE_URL = "http://localhost:3000/";

const JOBS_LIST_URL = `${API_BASE_URL}jobs`;

const APPLICANT_URL = `${API_BASE_URL}applicant`;

const PRODUCT_URL = `${API_BASE_URL}product`;

export { API_BASE_URL, JOBS_LIST_URL, APPLICANT_URL, PRODUCT_URL };
